<?xml version="1.0" encoding="utf-8" ?>
<d:BasePage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="clr-namespace:Translink.Pages;assembly=Translink.Pages"
             x:Class="Translink.Pages.RouteListPage">
  <RelativeLayout>
    <StackLayout>
      <Button
        x:Name="RefreshButton"
        HorizontalOptions="Fill"
        Command="{Binding RefreshRoutes}"
        Text="Refresh"/>
      <ListView ItemsSource="{Binding RouteList}" SelectedItem="{Binding SelectedRoute}">
        <ListView.ItemTemplate>
          <DataTemplate>
            <TextCell Text="{Binding Number}" Detail="{Binding StopWithDirection}"/>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </StackLayout>
    
    <ActivityIndicator 
      IsRunning="{Binding IsBusy}"
      VerticalOptions="CenterAndExpand"
      HorizontalOptions="CenterAndExpand"
      RelativeLayout.XConstraint="{ConstraintExpression Type=RelativeToParent,
        Property=Height,
        Factor=0.33}"
      RelativeLayout.YConstraint="{ConstraintExpression Type=RelativeToParent,
        Property=Height,
        Factor=0.33}"/>
  </RelativeLayout>
</d:BasePage>