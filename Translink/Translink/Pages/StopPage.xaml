<?xml version="1.0" encoding="utf-8" ?>
<d:BasePage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="clr-namespace:Translink.Pages;assembly=Translink.Pages"
             x:Class="Translink.Pages.StopPage">
    <StackLayout>
        <Button Text="Favourite" Command="{Binding FavouriteThisStop}"/>
        <Label Text="{Binding StopName}"/>
        <Label Text="{Binding StopNumber}"/>
        <Picker x:Name="RoutePicker" Title="Route" SelectedIndex="{Binding SelectedRouteIndex}"/>
        <ListView ItemsSource="{Binding Departures}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding AsString}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</d:BasePage>